name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: Platane/snk@v2
        with:
          github_user_name: tokagaz-vs
          
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            
          # 🐍 НОВЫЙ ПРАВИЛЬНЫЙ СПОСОБ ЗАДАТЬ ЦВЕТА
          # Все дополнительные параметры передаются через 'args' одной строкой
          args: --color_snake '#F72585' --color_dots '#fad1e8,#f8b4d9,#f58dc3,#f266ad,#F72585'

      - name: Push to repo
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "Generate Contribution Snake" || exit 0
          git push
